\documentclass[12pt]{article}

% Dimensiones exactas para YouTube (1280x720 px)
\usepackage[paperwidth=1280pt, paperheight=720pt, margin=0pt]{geometry}
\usepackage[dvipsnames]{xcolor}
\usepackage{graphicx}
\usepackage{fontspec}
\usepackage{tikz}

% --- CONFIGURACIÓN DE FUENTE ---
\setmainfont{Inter}[BoldFont=* Bold]

% --- VARIABLES DEL RETO ---
\newcommand{\numeroReto}{10º}
\newcommand{\tituloSerie}{reto de demostración con Lean 4}
\newcommand{\descripcionReto}{Si aₙ → L con L ≠ 0, entonces |aₙ| ≥ |L|/2 eventualmente}

% Espaciado de palabras
\newcommand{\espacioPalabras}{\spaceskip=0.25em \relax}

\definecolor{leanblue}{HTML}{007ACC}

\begin{document}
\pagestyle{empty}
\noindent
\begin{tikzpicture}[x=1pt, y=1pt, inner sep=0pt, outer sep=0pt]
    % Definir el área de trabajo exacta para evitar saltos de página
    \useasboundingbox (0,0) rectangle (1280,720);

    % Fondo azul total
    \fill[leanblue] (0,0) rectangle (1280,720);

    % --- 1. TÍTULO (ARRIBA) ---
    \node[anchor=north, text width=1200pt, align=center] at (640, 690) {
        {\fontsize{54pt}{65pt}\selectfont \espacioPalabras \bfseries \color{white} \numeroReto\ \tituloSerie}
    };

    % --- 2. LOGO (CENTRO) ---
    % Limitamos el alto para asegurar que el texto de abajo quepa
    \node[anchor=center] at (640, 410) {
        \includegraphics[width=1100pt, height=350pt, keepaspectratio]{logo.jpeg}
    };

    % --- 3. DESCRIPCIÓN (ABAJO) ---
    % Línea de acento naranja
    % \fill[Orange!90!black] (0, 200) rectangle (1280, 212);

    % Fondo azul para el área de texto (opcional, ya está el fondo general)
    \fill[leanblue] (0, 0) rectangle (1280, 200);

    % Reducimos el tamaño de la fuente de la descripción a 38pt
    \node[anchor=center, text width=1150pt, align=center] at (640, 100) {
        {\fontsize{38pt}{48pt}\selectfont \espacioPalabras \bfseries \color{white} \descripcionReto}
    };

\end{tikzpicture}
\end{document}

%%% Local Variables:
%%% mode: LaTeX
%%% TeX-master: t
%%% End:
